models:
  - name: stg_clientes
    description: "Tabela que identifica e unifica os clientes da Adventure Works (B2C e B2B)."
    columns:
      - name: id_cliente
        description: "Chave primária do cliente."
        tests:
          - unique
          - not_null